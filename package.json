{
  "name": "subplayer",
  "version": "0.1.0",
  "description": "simple mplayer interface to stop and repeat a video after a subtitle line",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "vigliag",
  "license": "ISC",
  "private": true,
  "dependencies": {
  }
}
